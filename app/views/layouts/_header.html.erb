<nav class="navbar navbar-default navbar-fixed-top" data-turbolinks="false">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"
        data-toggle="collapse" data-target="#navbar"
        aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_user && !current_user.trainee? %>
        <%= link_to admin_root_path, class: "logo" do %>
          <%= image_tag "training.jpg", title: t("project") %>
        <% end %>
        <%= link_to t("project"), admin_root_path, class: "navbar-brand" %>
      <% else %>
        <%= link_to root_url, class: "logo" do %>
          <%= image_tag "training.jpg", title: t("project") %>
        <% end %>
        <%= link_to t("project"), root_path, class: "navbar-brand" %>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <li><%= link_to t("layout.chat"), conversations_path %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                aria-haspopup="true" aria-expanded="false"><%= current_user.name %>
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to t("layout.profile"), current_user %></li>
                <li><%= link_to t("layout.edit_user"), edit_user_path(current_user) %></li>
                <li><%= link_to t("layout.logout"),
                  destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to t("layout.course"), courses_path %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                aria-haspopup="true" aria-expanded="false"><%= current_user.name %>
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to t("layout.profile"), current_user %></li>
                <li><%= link_to t("layout.edit_user"), edit_user_path(current_user) %></li>
                <li><%= link_to t("layout.feedback"), feedbacks_path %></li>
                <li><%= link_to t("layout.logout"),
                  destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
